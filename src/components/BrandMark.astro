---
import { PERSON } from "../consts";

const initials = `${PERSON.givenName[0]}${PERSON.familyName[0]}`;
---

<a class="brand-mark" href="/" aria-label={PERSON.fullName}>
	<span aria-hidden="true">{initials}</span>
</a>
